= link_to 'Add New Item', new_admin_item_path
%br
- @items.each do |i|
  .large-4.columns
    %b
      = i.name
    %br
    = i.description
    %br
      - unless i.sizes.nil?
        - i.sizes.each do |size|
          = size.name
          = size.price
        = link_to 'Edit', edit_admin_item_path(i)
